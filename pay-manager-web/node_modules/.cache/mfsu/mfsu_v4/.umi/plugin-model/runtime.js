import React from "react";
import { Provider } from "./";
import { models as rawModels } from "./model";
function ProviderWrapper(props) {
  const models = React.useMemo(() => {
    return Object.keys(rawModels).reduce((memo, key) => {
      memo[rawModels[key].namespace] = rawModels[key].model;
      return memo;
    }, {});
  }, []);
  return /* @__PURE__ */ React.createElement(Provider, { models, ...props }, props.children);
}
export function dataflowProvider(container, opts) {
  return /* @__PURE__ */ React.createElement(ProviderWrapper, { ...opts }, container);
}
