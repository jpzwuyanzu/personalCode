import { Card, Carousel } from "antd";
import { useState } from "react";
import { useAsyncEffect } from "ahooks";
import services from "@/services";
export default () => {
  const [noticeList, setNoticeList] = useState([]);
  useAsyncEffect(async () => {
    const items = await services.system.overview.getNoticeList();
    setNoticeList(items);
  }, []);
  return /* @__PURE__ */ React.createElement(Card, { bordered: true, className: "notice-card" }, noticeList?.length ? /* @__PURE__ */ React.createElement(Carousel, { autoplay: true, autoplaySpeed: 10 * 1e3, effect: "fade" }, noticeList.map((item, index) => {
    return /* @__PURE__ */ React.createElement("div", { key: index }, "\u516C\u544A\uFF1A", item?.content);
  })) : /* @__PURE__ */ React.createElement("span", null, "\u6682\u65E0\u516C\u544A"));
};
