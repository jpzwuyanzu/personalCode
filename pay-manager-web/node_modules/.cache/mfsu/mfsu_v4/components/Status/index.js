import {
  CallbackStatusOptions,
  PayStatusOptions,
  WithdrawAuditStatusOptions
} from "@/constants";
var Status = /* @__PURE__ */ ((Status2) => {
  Status2["pay"] = "pay";
  Status2["callback"] = "callback";
  Status2["withdrawApproval"] = "withdraw-approval";
  return Status2;
})(Status || {});
const statusMap = {};
const statusOptions = {
  ["pay" /* pay */]: PayStatusOptions,
  ["callback" /* callback */]: CallbackStatusOptions,
  ["withdraw-approval" /* withdrawApproval */]: WithdrawAuditStatusOptions
};
Object.keys(statusOptions).forEach((key) => {
  statusMap[key] = {};
  statusOptions[key].forEach((item) => {
    statusMap[key][item.value] = item;
  });
});
export default (props) => {
  const statusItem = statusMap?.[props.type]?.[props.val];
  return /* @__PURE__ */ React.createElement("span", { style: { color: statusItem?.color || "inherit" } }, statusItem?.label);
};
