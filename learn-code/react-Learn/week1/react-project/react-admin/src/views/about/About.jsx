import React from 'react';
import { Divider } from 'antd'
const About = () => {
    return (
        <>
            <div className="aboutTitle">
            <h1>关于项目</h1>
            <Divider/>
            <p>使用react + react-router + redux + react-redux + redux-thunk + antd + braft-editor + echarts 等技术实现。</p>
        </div>
        </>
    );
}

export default About;
